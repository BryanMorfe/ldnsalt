# ldnsalt – Local DNS Alter

## Description
Allows to alter the local DNS (hosts) of a local computer with ease (supports mounted drives of OSes). Currently supports altering Windows, Unix, Linux, and macOS.
